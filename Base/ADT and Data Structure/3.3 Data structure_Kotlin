Kotlin — ADT → Data Structures

Абстрактные типы данных (ADT)
│
├── 1. Sequences (Последовательности) — ADT
│
│   ├── Mutable Sequences — ADT
│   │       Kotlin:
│   │       ├── MutableList (ADT-интерфейс)
│   │       │       Реализации (структуры данных):
│   │       │       ├── ArrayList — динамический массив (динамический массив)
│   │       │       └── LinkedList — двусвязный список (Java)
│   │       │
│   │       └── MutableList<T> (но фактически 99% — ArrayList)
│   │
│   └── Immutable Sequences (read-only) — ADT
│           Kotlin:
│           ├── List (read-only интерфейс)
│           │       Реализации:
│           │       ├── listOf(...) — неизменяемое представление, подкладывает ArrayList
│           │       ├── emptyList() — пустая оптимизированная структура
│           │       └── listOf(1,2,3) → обычный ArrayList в immutable view
│
├── 2. Set Types (Множества) — ADT
│
│       Kotlin:
│       ├── MutableSet (ADT)
│       │       Реализации (структуры):
│       │       ├── HashSet — хеш-таблица
│       │       ├── LinkedHashSet — порядок вставки (hash + linked list)
│       │       └── TreeSet — сбалансированное дерево (красно-чёрное)
│       │
│       └── Set (read-only view)
│               Реализации:
│               ├── setOf(...) — часто HashSet
│               └── emptySet() — пустое множество
│
├── 3. Mapping Types (Словари / Ассоциативные массивы) — ADT
│
│       Kotlin:
│       ├── MutableMap (ADT)
│       │       Реализации:
│       │       ├── HashMap — хеш-таблица
│       │       ├── LinkedHashMap — порядок вставки
│       │       └── TreeMap — красно-чёрное дерево
│       │
│       └── Map (read-only interface)
│               Реализации:
│               ├── mapOf(...) — обычно LinkedHashMap
│               └── emptyMap()
│
├── 4. Queue Types (FIFO) — ADT
│
│       Kotlin не имеет собственной Queue,
│       используется java.util.Queue:
│
│       Реализации:
│       ├── ArrayDeque — двусторонняя очередь (deque)
│       ├── LinkedList — очередь на связанном списке
│       ├── PriorityQueue — очередь с приоритетами (min-heap)
│       ├── ConcurrentLinkedQueue — lock-free очередь
│       └── ArrayBlockingQueue / LinkedBlockingQueue — блокирующие очереди
│
├── 5. Stack Types (LIFO) — ADT
│
│       Kotlin рекомендует использовать ArrayDeque
│
│       Реализации:
│       ├── ArrayDeque — быстрый стек
│       └── Stack — устаревший Java-класс
│
├── 6. Priority Queue Types — ADT
│
│       Реализации:
│       └── PriorityQueue — бинарная куча
│
├── 7. Tree Types — ADT
│
│       Реализации:
│       ├── TreeSet — красно-чёрное дерево
│       └── TreeMap — красно-чёрное дерево
│
└── 8. Graph Types — ADT
        В стандартной библиотеке Kotlin нет.
        Типичные реализации вручную:
        ├── adjacency list — Map<Vertex, MutableList<Vertex>>
        ├── adjacency matrix — Array<Array<Int>>
        └── edge list — List<Pair<V, V>>



📌 Краткое резюме по Kotlin
| ADT (Абстрактный тип данных) | Kotlin интерфейс   | Реализации (структуры данных)         |
| ---------------------------- | ------------------ | ------------------------------------- |
| Sequence                     | List / MutableList | ArrayList, LinkedList                 |
| Set                          | Set / MutableSet   | HashSet, LinkedHashSet, TreeSet       |
| Mapping                      | Map / MutableMap   | HashMap, LinkedHashMap, TreeMap       |
| Queue                        | Queue (Java)       | ArrayDeque, LinkedList, PriorityQueue |
| Stack                        | —                  | ArrayDeque, (Stack — legacy)          |
| Priority Queue               | PriorityQueue      | PriorityQueue                         |
| Tree                         | —                  | TreeMap, TreeSet                      |
| Graph                        | —                  | Map+List, матрицы, списки рёбер       |
