Python — ADT → Data Structures (иерархия)

Абстрактные типы данных (ADT)
│
├── 1. Sequences (Последовательности) — ADT
│       - упорядоченные
│       - индексируемые
│
│   ├── Mutable Sequences (Изменяемые последовательности) — ADT
│   │       Реализации:
│   │       ├── list — список (dynamic array)
│   │       ├── bytearray — массив байт
│   │       └── array.array — массив чисел (C array)
│   │
│   └── Immutable Sequences (Неизменяемые последовательности) — ADT
│           Реализации:
│           ├── tuple — кортеж (fixed array)
│           ├── bytes — неизменяемый байтовый массив
│           └── str — строка (sequence of characters)
│
├── 2. Set Types (Множества) — ADT
│       - без повторов
│
│       Реализации:
│       ├── set — хеш-множество
│       └── frozenset — неизменяемое хеш-множество
│
├── 3. Mapping Types (Словари) — ADT
│       - ключ → значение
│
│       Реализации:
│       ├── dict — хеш-таблица
│       ├── defaultdict — хеш-таблица + значения по умолчанию
│       ├── OrderedDict — порядок вставки (двусвязный список + hashmap)
│       └── Counter — частотный словарь
│
├── 4. Queue Types (FIFO) — ADT
│
│       Реализации:
│       ├── collections.deque — двусторонняя очередь (двухсвязный список + блоки)
│       ├── queue.Queue — потокобезопасная очередь
│       ├── multiprocessing.Queue — межпроцессная очередь
│       └── SimpleQueue — упрощённая очередь
│
├── 5. Stack Types (LIFO) — ADT
│
│       Реализации:
│       ├── list (append/pop) — стек на динамическом массиве
│       └── collections.deque — стек на двусвязной очереди
│
├── 6. Priority Queue Types (Приоритетные очереди) — ADT
│
│       Реализации:
│       ├── heapq — бинарная куча (min-heap)
│       └── queue.PriorityQueue — потокобезопасная куча
│
├── 7. Tree Types (Деревья) — ADT
│       - нет в стандартной библиотеке напрямую
│
│       Типовые реализации (через сторонние пакеты / Self-implemented):
│       ├── Binary Tree — дерево
│       ├── BST — бинарное дерево поиска
│       ├── AVL / Red-Black Tree — сбалансированные BST
│       └── Trie — префиксное дерево
│
└── 8. Graph Types (Графы) — ADT
        - нет в стандартной библиотеке
        Реализации:
        ├── adjacency list — словарь списков
        ├── adjacency matrix — матрица
        └── edge list — список рёбер
