Абстрактные типы данных (ADT) — абстракции поведения
│
├── 1. Sequences (Последовательности) — ADT
│       - Элементы имеют порядок
│       - Доступ по индексу (у большинства)
│       - Могут быть изменяемыми или неизменяемыми
│
│   ├── Mutable Sequences (Изменяемые последовательности) — ADT
│   │       Реализации (структуры данных):
│   │
│   │            ├── List<T> (Список) — структура данных
│   │            │       - Базируется на массиве (array)
│   │            │       - Быстрый доступ по индексу
│   │
│   │            ├── LinkedList<T> (Связный список) — структура данных
│   │            │       - Узлы со ссылками
│   │            │       - Быстрое добавление/удаление
│   │
│   │            ├── ObservableCollection<T> — структура данных
│   │            │       - События при изменении
│   │
│   │            └── Array (Массив) — структура данных
│   │                    - Фиксированный размер
│   │
│   └── Immutable Sequences (Неизменяемые последовательности) — ADT
│           Реализации:
│
│           ├── ImmutableList<T> — структура данных
│           ├── ImmutableArray<T> — структура данных
│           └── ImmutableQueue<T> — структура данных
│
├── 2. Set Types (Множества) — ADT
│       - Нет повторяющихся значений
│       - Не гарантируют порядок (кроме SortedSet)
│
│           Реализации (структуры данных):
│
│           ├── HashSet<T> — структура данных
│           │       - Хеш-таблица
│           │
│           ├── SortedSet<T> — структура данных
│           │       - Красно-чёрное дерево (red-black tree)
│           │       - Естественный сортированный порядок
│           │
│           └── ImmutableHashSet<T>, ImmutableSortedSet<T> — структуры данных
│
├── 3. Mapping Types (Словари / Ассоциативные массивы) — ADT
│       - Пары ключ → значение
│       - Быстрый поиск по ключу
│
│           Реализации (структуры данных):
│
│           ├── Dictionary<TKey,TValue> — структура данных
│           │       - Хеш-таблица
│           │
│           ├── SortedDictionary<K,V> — структура данных
│           │       - Красно-чёрное дерево
│           │
│           ├── ConcurrentDictionary<K,V> — структура данных
│           │       - Потокобезопасная хеш-таблица
│           │
│           └── ImmutableDictionary<K,V>, ImmutableSortedDictionary<K,V>  — структура данных
│
├── 4. Queue Types (Очереди FIFO) — ADT
│       - First-In-First-Out
│
│           Реализации:
│
│           ├── Queue<T> — структура данных
│           │       - Кольцевой буфер (ring buffer)
│           │
│           ├── ConcurrentQueue<T> — структура данных
│           └── ImmutableQueue<T> — структура данных
│
└── 5. Priority Queue Types (Очереди с приоритетами, HIFO/MinHeap) — ADT
│        - Элемент выбирается по приоритету
│
│           Реализации:
│
│           └── PriorityQueue<T,TPriority> — структура данных
│                - Бинарная куча (binary heap)
│
├── 6. Stack Types (Стек LIFO) — ADT
│       - Last-In-First-Out
│
│           Реализации:
│
│           └── Stack<T> — структура данных
│                - Внутри динамический массив
