✅ 1. Что такое “Data Type” (тип данных) — простыми словами https://en.wikipedia.org/wiki/Data_type
Тип данных — это математическое и языковое понятие, которое определяет:
✔ Какие значения могут существовать
    (например: целые числа, строки, булевы значения)
✔ Какие операции разрешены
    (сложение для чисел, конкатенация для строк)
✔ Как интерпретировать и хранить данные
    (размерность, диапазон, точность, кодировка, представление)

⭐ Интуитивно
Тип данных = ограничение и смысл данных.
    int → означает «это целое число, его можно складывать, умножать и т.п.»
    string → «это последовательность символов»

❗ Это важно
Тип данных — это абстракция языка, а НЕ структура хранения!
    Тип данных говорит что это такое, а не как это устроено внутри.



✅ 2. Что такое “Abstract Data Type" (ADT) — по Википедии https://en.wikipedia.org/wiki/Abstract_data_type
ADT — математическая модель данных, определённая поведением:
какими операциями тип должен обладать и какими правилами эти операции руководствуются.

ADT определяет:
✔ какие есть операции
    (push, pop, enqueue, search, insert)
✔ какие правила (семантика)
    для стека: LIFO
    для очереди: FIFO
    для множества: нет дубликатов, порядок неважен
✔ но НЕ определяет, как хранить
    Стек можно реализовать и массивом, и списком.

⭐ Интуитивно
ADT = ролевой сценарий + правила, но не механизм.
    Стек = «последний вошёл — первый вышел».
    Очередь = «первый вошёл — первый вышел».
    Какие структуры используются для этого — не важно.



🚫 ADT ≠ Data Type
Они решают разные задачи:
| Что описывает                        | Data Type  | ADT                  |
| ------------------------------------ | ---------- | -------------------- |
| **Какие значения возможны**          | ✔ Да       | ❌ Нет                |
| **Какие операции допустимы**         | ✔ Да       | ✔ Да                 |
| **Семантические правила поведения**  | Иногда     | ✔ Основное           |
| **Как хранится в памяти**            | Иногда     | ❌ Нет                |
| **Может иметь несколько реализаций** | Обычно нет | ✔ Да (ключевая идея) |



🎯 Короткое резюме
👉 Data Type = набор значений + допустимые операции + представление
    (например: int, float, string, boolean)
👉 ADT = поведенческая спецификация, операция + правила работы
    (например: Stack, Queue, Set, Map)


⭐ Ещё более коротко
📌 Тип данных — это что это такое.
    Например: «число», «строка».
📌 ADT — это как этим пользоваться.
    Например: «стек», «очередь», «множество».


🧩 Пример: стек
Data Type?
    Нет!
    Стек — это не тип данных языка, в языке нет слова stack.

ADT?
    Да!
    Потому что стек определён правилами:
      push,
      pop,
      last-in-first-out.

Data structure?
    Можно реализовать:
      массивом,
      связным списком.



🧩 Пример: int
Data Type?
    Да.

ADT?
    Нет — у него нет абстрактного поведения типа LIFO/FIFO.

Data structure?
    Нет — это просто базовый тип.


✔ Итоговое различие (одно предложение)
    Тип данных — это набор допустимых значений и операций, а ADT — это набор операций + поведенческие правила без указания реализации.
